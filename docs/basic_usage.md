# Basic usage

At the moment each submodule can be run on its own by calling the according main function under `__main__.py`.  
Ideally at a later stage one could execute the whole pipeline via a single entry point.  
The development is still ongoing and this might be implemented at a later stage.

TODO: add more info

